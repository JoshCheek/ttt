#!/usr/bin/env ruby
$:.unshift File.expand_path('../../lib', __FILE__)
require 'ttt'


game = TTT::Game.new

until game.over?
  puts game.board(:ttt)
  game.mark gets.to_i
end

puts "The game is over."
if game.tie?
  puts "No one wins"
else
  puts "Player #{game.winner} wins."
end
